{
  "name": "boersenblick-website",
  "version": "0.1.0",
  "private": true,
  "description": "Statische deutsche Finanz-Nachrichtenseite (Vercel). Kein Framework, keine Abhängigkeiten.",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node scripts/serve.js dist 3210"
  },
  "engines": {
    "node": ">=18"
  }
}
